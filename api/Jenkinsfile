pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
                sh 'docker network create -d bridge kona'
                sh ' docker network ls'
   
            }
        }
    }
}
